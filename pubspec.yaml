name: truck_scale
description: Truck Scale desktop app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-170.0.dev <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  # desktop designs
  bitsdojo_window: ^0.1.2
  desktop: ^3.1.5 # https://adrianos42.github.io/desktop/#/
  # database
  drift: ^1.7.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.0.0
  path: ^1.8.2
  shared_preferences: ^2.0.15
  # bloc
  flutter_bloc: ^8.0.1
  bloc: ^8.0.3
  equatable: ^2.0.3
  #styles
  google_fonts: ^3.0.1
  flutter_screenutil: ^5.5.3+2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
